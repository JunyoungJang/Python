# When a module is imported, Python:
#   1. Executes the statements it contains.
#   2. Creates an object that stores reference to the top-level items in that module

import noisy

print noisy.NOISE_LEVEL
